# Nautilus 前后端对接实施指南

**日期**: 2026-03-11
**基于**: 前后端对接验证报告
**目标**: 完成剩余60%的对接工作

---

## 执行摘要

本指南提供了完成Nautilus前后端对接的详细步骤。已完成40%的准备工作（类型定义、增强版客户端、错误边界），剩余60%需要按本指南执行。

**预计时间**: 3-4小时
**优先级**: P0 (紧急)

---

## 快速开始

### 已创建的文件（可直接使用）

```
phase3/frontend/src/types/api.ts                # 完整的API类型定义
phase3/frontend/src/lib/api-enhanced.ts             # 增强版API客户端
phase3/frontend/src/lib/websocket-enhanced.ts    # 增强版WebSocket客户端
phase3/frontend/src/components/common/ErrorBoundary.tsx  # 错误边界组件
```

### 立即执行的步骤

#### Step 1: 备份旧文件
```bash
cd /c/Users/chunx/Projects/nautilus-core/phase3/frontend/src

cp lib/api.ts lib/api.ts.backup
cp lib/websocket.ts lib/websocket.ts.backup
```

#### Step 2: 替换为增强版
```bash
mv lib/api-enhanced.ts lib/api.ts
mv lib/websocket-enhanced.ts lib/websocket.ts
```

#### Step 3: 更新App.tsx
在App.tsx中添加ErrorBoundary包裹整个应用。

---

## Phase 1: 统一字段命名 (2小时)

### 需要修改的文件

1. **AgentsPage.tsx** - 删除本地Agent接口，使用AgentResponse
2. **AgentDetailPage.tsx** - 添加区块链状态显示
3. **TaskDetailPage.tsx** - 添加完整的任务信息显示

### 关键修改点

- 使用 `parseSpecialties()` 转换specialties字符串为数组
- 使用 `calculateSuccessRate()` 计算成功率
- 使用 `formatAddress()` 格式化钱包地址
- 使用 `weiToEth()` 转换Wei为ETH

---

## Phase 2: 添加新功能 (1小时)

### 1. 区块链状态显示

在AgentDetailPage中添加区块链状态标签页，显示：
- 数据库注册状态
- 区块链注册状态
- 链上注册状态
- 交易哈希链接

### 2. Gas费用显示

在TaskDetailPage中添加Gas信息，显示：
- Gas使用量
- Gas成本（ETH）
- Gas分摊比例

### 3. 任务超时倒计时

计算并显示任务剩余时间。

---

## Phase 3: 测试 (1小时)

### 测试清单

#### API客户端测试
- [ ] Agent列表正常显示
- [ ] Agent详情显示所有字段
- [ ] 网络错误自动重试
- [ ] 错误提示友好

#### WebSocket测试
- [ ] 连接成功
- [ ] 心跳正常（每30秒）
- [ ] 断线自动重连
- [ ] 消息队列正常

#### 错误边界测试
- [ ] 捕获组件错误
- [ ] 显示友好错误UI
- [ ] 重试功能正常
- [ ] 刷新和返回首页正常

---

## 验收标准

### 功能完整性
- [ ] 所有后端字段前端正确使用
- [ ] 字段命名完全一致
- [ ] 类型定义与API响应匹配

### 稳定性
- [ ] WebSocket连接稳定
- [ ] 错误处理统一友好
- [ ] 断线重连正常工作
- [ ] 重试机制正常工作

### 用户体验
- [ ] 加载状态完整流畅
- [ ] 错误提示清晰友好
- [ ] 响应速度满足要求

---

## 常见问题

### Q1: 类型错误
使用新的AgentResponse类型替代本地Agent接口。

### Q2: API调用失败
正确解构响应：`const { data } = await api.get()`

### Q3: WebSocket不重连
确保使用了websocket-enhanced.ts。

---

**创建时间**: 2026-03-11
**状态**: 准备就绪，可立即执行
