# 🧪 测试补充计划 - 70% → 80%

**开始时间**: 2026-03-11 04:25
**当前覆盖率**: 70%
**目标覆盖率**: 80%

---

## 📊 当前状态

**已有测试** (3个文件):
- ✅ authStore.test.ts (5 tests)
- ✅ taskStore.test.ts (8 tests)
- ✅ uiStore.test.ts (9 tests)

**总计**: 22个测试，100%通过

---

## 🎯 需要补充的测试

### 优先级1: Hooks测试

**文件**: `src/hooks/useAuth.ts`
- 测试文件: `src/hooks/__tests__/useAuth.test.ts`
- 测试用例:
  - 登录功能
  - 登出功能
  - 用户状态管理
  - Token处理

### 优先级2: 组件测试

**1. Button组件**
- 文件: `src/components/common/Button.tsx`
- 测试: 点击事件、样式、禁用状态

**2. Card组件**
- 文件: `src/components/common/Card.tsx`
- 测试: 渲染、props传递

**3. TaskCard组件**
- 文件: `src/components/task/TaskCard.tsx`
- 测试: 任务显示、状态变化

### 优先级3: Context测试

**AuthContext**
- 文件: `src/context/AuthContext.tsx`
- 测试: Provider、Consumer、状态管理

---

## 📋 执行计划

### Step 1: 创建hooks测试 (30分钟)

```typescript
// src/hooks/__tests__/useAuth.test.ts
import { renderHook } from '@testing-library/react'
import { useAuth } from '../useAuth'

describe('useAuth', () => {
  it('should handle login', () => {
    // 测试登录逻辑
  })

  it('should handle logout', () => {
    // 测试登出逻辑
  })
})
```

### Step 2: 创建组件测试 (1小时)

```typescript
// src/components/common/__tests__/Button.test.tsx
import { render, fireEvent } from '@testing-library/react'
import Button from '../Button'

describe('Button', () => {
  it('should render correctly', () => {
    // 测试渲染
  })

  it('should handle click', () => {
    // 测试点击
  })
})
```

### Step 3: 创建context测试 (30分钟)

```typescript
// src/context/__tests__/AuthContext.test.tsx
import { render } from '@testing-library/react'
import { AuthProvider } from '../AuthContext'

describe('AuthContext', () => {
  it('should provide auth state', () => {
    // 测试状态提供
  })
})
```

---

## 🎯 预期结果

**新增测试**:
- Hooks测试: 5个
- 组件测试: 10个
- Context测试: 3个

**总计**: 40个测试（当前22个 + 新增18个）

**预期覆盖率**: 80%+

---

## ⏱️ 时间估算

- Hooks测试: 30分钟
- 组件测试: 1小时
- Context测试: 30分钟
- 调试和优化: 30分钟

**总计**: 2.5小时

---

## 🚀 开始执行

**当前任务**: 创建useAuth测试

**下一步**:
1. 创建hooks测试目录
2. 编写useAuth测试
3. 运行测试验证
4. 继续补充其他测试

---

**开始补充测试！** 🧪
